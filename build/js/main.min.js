/******/ (function(modules) { // webpackBootstrap
/******/ 	// The module cache
/******/ 	var installedModules = {};
/******/
/******/ 	// The require function
/******/ 	function __webpack_require__(moduleId) {
/******/
/******/ 		// Check if module is in cache
/******/ 		if(installedModules[moduleId]) {
/******/ 			return installedModules[moduleId].exports;
/******/ 		}
/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = installedModules[moduleId] = {
/******/ 			i: moduleId,
/******/ 			l: false,
/******/ 			exports: {}
/******/ 		};
/******/
/******/ 		// Execute the module function
/******/ 		modules[moduleId].call(module.exports, module, module.exports, __webpack_require__);
/******/
/******/ 		// Flag the module as loaded
/******/ 		module.l = true;
/******/
/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}
/******/
/******/
/******/ 	// expose the modules object (__webpack_modules__)
/******/ 	__webpack_require__.m = modules;
/******/
/******/ 	// expose the module cache
/******/ 	__webpack_require__.c = installedModules;
/******/
/******/ 	// define getter function for harmony exports
/******/ 	__webpack_require__.d = function(exports, name, getter) {
/******/ 		if(!__webpack_require__.o(exports, name)) {
/******/ 			Object.defineProperty(exports, name, { enumerable: true, get: getter });
/******/ 		}
/******/ 	};
/******/
/******/ 	// define __esModule on exports
/******/ 	__webpack_require__.r = function(exports) {
/******/ 		if(typeof Symbol !== 'undefined' && Symbol.toStringTag) {
/******/ 			Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });
/******/ 		}
/******/ 		Object.defineProperty(exports, '__esModule', { value: true });
/******/ 	};
/******/
/******/ 	// create a fake namespace object
/******/ 	// mode & 1: value is a module id, require it
/******/ 	// mode & 2: merge all properties of value into the ns
/******/ 	// mode & 4: return value when already ns object
/******/ 	// mode & 8|1: behave like require
/******/ 	__webpack_require__.t = function(value, mode) {
/******/ 		if(mode & 1) value = __webpack_require__(value);
/******/ 		if(mode & 8) return value;
/******/ 		if((mode & 4) && typeof value === 'object' && value && value.__esModule) return value;
/******/ 		var ns = Object.create(null);
/******/ 		__webpack_require__.r(ns);
/******/ 		Object.defineProperty(ns, 'default', { enumerable: true, value: value });
/******/ 		if(mode & 2 && typeof value != 'string') for(var key in value) __webpack_require__.d(ns, key, function(key) { return value[key]; }.bind(null, key));
/******/ 		return ns;
/******/ 	};
/******/
/******/ 	// getDefaultExport function for compatibility with non-harmony modules
/******/ 	__webpack_require__.n = function(module) {
/******/ 		var getter = module && module.__esModule ?
/******/ 			function getDefault() { return module['default']; } :
/******/ 			function getModuleExports() { return module; };
/******/ 		__webpack_require__.d(getter, 'a', getter);
/******/ 		return getter;
/******/ 	};
/******/
/******/ 	// Object.prototype.hasOwnProperty.call
/******/ 	__webpack_require__.o = function(object, property) { return Object.prototype.hasOwnProperty.call(object, property); };
/******/
/******/ 	// __webpack_public_path__
/******/ 	__webpack_require__.p = "";
/******/
/******/
/******/ 	// Load entry module and return exports
/******/ 	return __webpack_require__(__webpack_require__.s = "./js/main.js");
/******/ })
/************************************************************************/
/******/ ({

/***/ "./js/main.js":
/*!********************!*\
  !*** ./js/main.js ***!
  \********************/
/*! no static exports found */
/***/ (function(module, exports) {

var sliderElement = document.querySelector('.swiper');

if (sliderElement) {
  var swiper = new Swiper('.swiper', {
    direction: 'horizontal',
    loop: true,
    navigation: {
      nextEl: '.slider__arrows-item--next',
      prevEl: '.slider__arrows-item--prew'
    },
    slidesPerView: 4,
    spaceBetween: 30,
    breakpoints: {
      320: {
        slidesPerView: 2,
        spaceBetween: 30,
        slidesPerGroup: 2,
        pagination: {
          el: '.slider__pagination',
          type: 'fraction',
          renderFraction: function renderFraction(currentClass, totalClass) {
            return '<span class="' + currentClass + '"></span>' + ' of ' + '<span class="' + totalClass + '"></span>';
          }
        }
      },
      768: {
        slidesPerView: 2,
        spaceBetween: 30,
        slidesPerGroup: 2,
        pagination: {
          el: '.slider__pagination',
          type: 'bullets',
          clickable: true,
          renderBullet: function renderBullet(index, className) {
            return '<span class="' + className + '">' + (index + 1) + '</span>';
          }
        }
      },
      1024: {
        slidesPerView: 4,
        spaceBetween: 30,
        slidesPerGroup: 4,
        pagination: {
          el: '.slider__pagination',
          type: 'bullets',
          clickable: true,
          renderBullet: function renderBullet(index, className) {
            return '<span class="' + className + '">' + (index + 1) + '</span>';
          }
        }
      }
    }
  });
}

var isEscKey = function isEscKey(evt) {
  return evt.key === 'Escape' || evt.key === 'Esc';
};

var faqAccordeonButtons = document.querySelectorAll('.faq__question-head button');

if (faqAccordeonButtons) {
  faqAccordeonButtons.forEach(function (element) {
    var parent = element.closest('.accordeon-item');
    element.addEventListener('click', function () {
      parent.classList.toggle('accordeon-item--open');
    });
  });
}

var menuButton = document.querySelector('.header__menu-button');
var page = document.querySelector('.page');

var removeFilterClass = function removeFilterClass() {
  page.classList.remove('page--filter-open');
};

if (menuButton) {
  menuButton.addEventListener('click', function () {
    page.classList.toggle('page--menu-open');
  });
}

var onEscCloseFilter = function onEscCloseFilter(evt) {
  if (isEscKey(evt)) {
    evt.preventDefault();
    removeFilterClass();
  }
};

var filter = document.querySelector('.filter-form');
var catalogControl = document.querySelector('.catalog__filter-control');

if (filter) {
  var fieldsetControl = filter.querySelectorAll('.filter-form__fieldset-control');
  var filterCloseButton = filter.querySelector('.filter-form__close-button');

  if (catalogControl) {
    catalogControl.addEventListener('click', function () {
      page.classList.add('page--filter-open');
      document.addEventListener('keydown', onEscCloseFilter);
    });
  }

  if (fieldsetControl) {
    fieldsetControl.forEach(function (element) {
      var parent = element.closest('fieldset');
      element.addEventListener('click', function () {
        parent.classList.toggle('filter-form__fieldset--open');
      });
    });
  }

  if (filterCloseButton) {
    filterCloseButton.addEventListener('click', function () {
      page.classList.remove('page--filter-open');
      document.removeEventListener('keydown', onEscCloseFilter);
    });
  }
}

var userInfo = {
  userEmail: ''
};

var testStorage = function testStorage() {
  try {
    userInfo.userEmail = localStorage.getItem('email');
    return true;
  } catch (err) {
    return false;
  }
};

var saveData = function saveData(form) {
  localStorage.setItem('email', form.querySelector('#email').value);
};

var loadData = function loadData(form) {
  if (testStorage()) {
    form.querySelector('#email').value = userInfo.userEmail;
  }
};

var closePopupKey = function closePopupKey(evt) {
  if (isEscKey(evt)) {
    page.classList.remove('page__login-popup--open');
    removeEventListener('click', closePopup);
    removeEventListener('keydown', closePopupKey);
  }
};

var closePopup = function closePopup(evt) {
  var target = evt.target;
  var loginForm = document.querySelector('.login__content');
  var loginCloseButton = document.querySelector('.login__close-button');
  var isForm = target === loginForm || loginForm.contains(target);
  var isButton = target === loginCloseButton || loginCloseButton.contains(target);

  if (!isForm || isButton) {
    page.classList.remove('page__login-popup--open');
    removeEventListener('click', closePopup);
    removeEventListener('keydown', closePopupKey);
  }
};

var loginHeadLink = document.querySelectorAll('.menu-login');

if (loginHeadLink) {
  loginHeadLink.forEach(function (element) {
    element.addEventListener('click', function (evt) {
      evt.preventDefault();
      page.classList.add('page__login-popup--open');
      var loginPopup = document.querySelector('#login-popup');
      var loginForm = loginPopup.querySelector('.login__form');
      var formEmail = loginForm.querySelector('#email');
      var loginSubmit = loginForm.querySelector('.login__submit-button');
      loadData(loginForm);
      formEmail.focus();

      var onLoginSubmit = function onLoginSubmit() {
        return saveData(loginForm);
      };

      loginPopup.addEventListener('click', closePopup);
      document.addEventListener('keydown', closePopupKey);
      loginSubmit.addEventListener('click', onLoginSubmit);
    });
  });
}

/***/ })

/******/ });
//# sourceMappingURL=main.min.js.map